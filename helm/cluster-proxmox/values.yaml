name: cluster-proxmox
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

image:
  registry: quay.io
  name: giantswarm/cluster-proxmox
  tag: v0.0.1
  pullPolicy: IfNotPresent
