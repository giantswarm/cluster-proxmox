apiVersion: v1
kind: Secret
metadata:
  name: {{ include "credentialSecretName" $ }}
  labels:
    {{- include "labels.common" . | nindent 4 }}
stringData:
  secret: {{ .Values.global.providerSpecific.proxmox.secret | quote }}
  token: {{ .Values.global.providerSpecific.proxmox.token | quote }}
  url: {{ .Values.global.providerSpecific.proxmox.url | quote }}
